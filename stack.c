#include "monty.h"
/**
 * f_stack - prints the stack to the top
 * @head:  stack
 * @counter: number line
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
